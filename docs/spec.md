# Техническое задание: «Змейка ест кроликов»

> Актуальная версия ТЗ. Обновляется по ходу разработки.
> Последнее обновление: 2026-02-12

---

## Оглавление

1. [Общие требования](#1-общие-требования)
2. [Правила игры](#2-правила-игры)
3. [Кролики](#3-кролики)
4. [Голод змейки](#4-голод-змейки)
5. [Победа / уровни / время](#5-победа--уровни--время)
6. [Генерация стен](#6-генерация-стен)
7. [Боты (AI)](#7-боты-ai)
8. [UI / экраны](#8-ui--экраны)
9. [Архитектура проекта](#9-архитектура-проекта)
10. [Игровой цикл (tick)](#10-игровой-цикл-tick)
11. [Тесты](#11-тесты)
12. [Этапы разработки и прогресс](#12-этапы-разработки-и-прогресс)

---

## 1. Общие требования

| Параметр | Значение |
|---|---|
| Язык | TypeScript |
| Сборка | webpack (dev server + production build) |
| Рендер | HTML5 Canvas |
| Тесты | Jest |
| Вёрстка | Адаптивная, поле по центру |
| Интерфейс | Полностью на русском языке |
| Стили | CSS-классы + CSS custom properties (без inline-стилей) |

### Представление поля (engine)

Двумерный массив `string[][]`:

| Символ | Значение |
|--------|----------|
| `" "` | Пусто |
| `"&"` | Кролик |
| `"*"` | Стена / преграда |
| `"#"` | Сегмент змейки |

Все коллизии и логика считаются по engine-состоянию, затем renderer рисует.

### Управление

| Игрок | Клавиши |
|-------|---------|
| Игрок 1 | W A S D |
| Игрок 2 | Стрелки |

> ⚠️ Уточнение: максимум **2 игрока** (не 3). Третьего игрока не бывает.

### Режимы

- Игроки: 0–2 на клавиатуре
- Боты: 0–4 автономных змейки
- 0 игроков = режим наблюдателя (боты играют, пользователь смотрит)

### Очки

- +1 очко за каждого съеденного кролика, отдельно по каждой змейке

### Результаты

- По окончании игры: запись в `localStorage`, вывод таблицы рекордов
- Имена игроков также сохраняются в `localStorage` (с выбором из ранее использованных через выпадающий список)

### Визуальный стиль

| Элемент | Цвет |
|---------|------|
| Фон | Чёрный (#000) |
| Сетка | Светло-серый (#cccccc) |
| Змейка 1 | Зелёный (#00FF00) |
| Змейка 2+ | Разные цвета (cyan, yellow, magenta, orange…) |
| Кролики | Красный (#FF0000) |
| Стены | Белый (#FFFFFF) |
| Голова змейки | Обводка белым |

### Размер поля

- Базовый: **40×40**
- Каждый следующий уровень: **+5** к ширине и высоте

---

## 2. Правила игры (классические)

### Смерть змейки

- Столкновение со стеной / преградой / выход за границы
- Столкновение с другой змейкой
- Столкновение с самой собой (самоукус)
- Голод (длина < 2)

### Движение

- Змейка не может двигаться назад (запрещён разворот на 180°)
- Движение на 1 клетку за тик

### Поедание

- Съела кролика → растёт на 1 сегмент, +1 очко, сброс счётчика голода

---

## 3. Кролики

### Начальный спавн

В начале уровня появляется кроликов: **`Math.floor((snakeCount × 1.5) + (10 − difficultyLevel))`**

- Не на стенах, не на змейках
- Минимум Chebyshev distance **> 1** между любыми двумя кроликами

### Размножение

Каждый кролик имеет:
- `clockNum` — число тактов с момента появления / последнего размножения
- `reproductionCount` — сколько раз размножился (макс. **5**)

**Окно размножения**: такты **5–15** (включительно)

**Вероятность за тик**: `0.05 × clockNum` (с модификаторами)

**Модификатор соседей** (Chebyshev distance ≤ 2):
- −25% за каждого соседнего кролика
- При 4+ соседях — вероятность = 0

**Потомство**:
- Новый кролик на расстоянии 1–2 клетки от родителя (случайное направление)
- Не на стенах/змейках
- Chebyshev distance > 1 от всех существующих кроликов

**После размножения**: `clockNum = 0`

---

## 4. Голод змейки

- Начальная длина: **5**
- Если не съела кролика за **15 тактов** → теряет 1 сегмент, сброс счётчика
- Если длина < **2** → смерть «Умерла с голоду»

---

## 5. Победа / уровни / время

### Одиночная игра (1 змейка)

- Цель на уровень: **`Math.floor(level × 1.2 + 10)`** кроликов
- Счёт **кумулятивный**: очки копятся через уровни, цель — сумма целей всех уровней
  - Уровень 1: нужно 11 кроликов
  - Уровень 2: нужно ещё 12, итого ≥ 23
  - Уровень 3: нужно ещё 13, итого ≥ 36
- Набрал кумулятивную цель → следующий уровень

### Многопользовательская игра (>1 змейки)

- Уровень заканчивается когда:
  - Осталась **1** змейка — она побеждает
  - Истекло **3 минуты** — ничья (если живы обе)
  - Все погибли — нет победителя

- **Счётчик побед**: у каждой змейки отдельно считаются выигранные уровни

### Определение итогового победителя

> ⚠️ Уточнение:
> 1. Победитель уровня — **выживший**
> 2. Если после 3 минут обе живы — **ничья**, побед никому не начисляется
> 3. Итоговый победитель: у кого **больше побед**
> 4. Если побед поровну — у кого **больше съеденных кроликов (очков)**

---

## 6. Генерация стен

### Параметры

| Формула | Описание |
|---------|----------|
| `Math.floor(level × 1.2 + 2)` | Количество кластеров стен |
| `Math.floor(difficultyLevel × 1.2 + 3)` | Длина стены (сегментов) |

### Алгоритм

- Random walk + ветвления (30% вероятность ответвления)
- Стены не на краях поля (минимум 1 клетка от границы)
- Стены генерируются **до** кроликов
- Кролики **не спавнятся** на стенах

### Ограничения

> ⚠️ Уточнение:
> - Стены не должны создавать изолированные комнаты
> - Змейка должна мочь зайти и выйти — минимум **2 прохода**
> - Проверка: **BFS** — все свободные клетки должны быть связаны
> - Если генерация не прошла проверку → **перегенерация** (до 50 попыток, иначе пустые стены)

### difficultyLevel

- Диапазон: **1–10** (выбирается в меню)
- По умолчанию уровень 1

---

## 7. Боты (AI)

### Интерфейс

```typescript
type BotDecision = 'left' | 'right' | 'front';

interface BotInput {
  vision: number[][];        // 20×20, повёрнутый относительно головы
  snakeLength: number;
  ticksWithoutFood: number;
}

function botDecide(input: BotInput): BotDecision;
```

### Формирование vision (20×20)

- Матрица повёрнута так, что направление змейки всегда «вверх» (row 0)
- Утилита `rotateToWorld` переводит координаты vision → мир

**Сигналы препятствий** (стены, тела змеек) — отрицательные:

| Расстояние | Сигнал |
|-----------|--------|
| 1 клетка | −100 |
| 2 клетки | −80 |
| 3 клетки | −60 |
| … | Затухание на 20 за клетку |
| Очень далеко (dist d) | `−max(500/d, 5)` |

Сигналы от нескольких препятствий **суммируются**.

**Сигналы кроликов** — положительные:

| Расстояние | Сигнал |
|-----------|--------|
| 1 клетка | +100 |
| 2 клетки | +80 |
| … | Затухание на 20 за клетку |
| Минимум | **+5** (на любом расстоянии) |

### Эвристический алгоритм

1. Оценка 3 ходов (left, front, right)
2. Запрет заведомой смерти (`−Infinity`)
3. Flood-fill / оценка доступной области (анти-тупик)
4. Притяжение к кроликам
5. Штраф за приближение к стенам/телам
6. Учёт длины: короче → агрессивнее к кроликам

---

## 8. UI / экраны

### Общие требования

- Весь интерфейс на **русском языке**
- Стили через **CSS-классы** и **CSS custom properties** (не inline)
- Цвета определены как CSS-переменные в `:root`

### Меню

- Поля: Игроки (0–2), Боты (0–4), Имя игрока 1, Имя игрока 2, Сложность (1–10)
- Вёрстка: **grid**, лейблы слева, поля ввода справа (выровнены по правому краю)
- Кнопка «СТАРТ» по центру
- У полей имён — кнопка **▼** для выбора из ранее сохранённых имён (localStorage)

### Игра

- Canvas по центру
- HUD сверху: уровень, цель, время, статус каждой змейки (очки / длина / жив-мёртв)

### Результаты

- Таблица итогов: имя, очки, победы, статус
- Таблица рекордов из localStorage
- Кнопки: «Заново», «Меню»

---

## 9. Архитектура проекта

```
src/
├── index.ts                    # Точка входа, управление экранами и game loop
├── index.html                  # HTML-шаблон (без inline-стилей)
├── app/
│   ├── router.ts               # Навигация между экранами
│   ├── inputHandler.ts         # Обработка клавиатуры (WASD + стрелки)
│   └── ui/
│       ├── menuView.ts         # Экран меню
│       ├── hudView.ts          # HUD в игре
│       ├── resultView.ts       # Экран результатов
│       └── styles.css          # Все стили (CSS custom properties)
├── engine/
│   ├── types.ts                # Типы (Direction, Snake, Rabbit, GameState…)
│   ├── constants.ts            # Константы (размеры, тайминги, пороги)
│   ├── game.ts                 # Создание состояния, initLevel, processTick
│   ├── board.ts                # createEmptyBoard, buildBoard, inBounds
│   ├── collision.ts            # Коллизии (стена, змейка, самоукус, запрет назад)
│   ├── spawning/
│   │   ├── wallsGenerator.ts   # Генерация стен (random walk + BFS)
│   │   └── rabbitsSpawner.ts   # Спавн кроликов
│   └── systems/
│       ├── movementSystem.ts   # Движение змейки
│       ├── hungerSystem.ts     # Голод / усыхание / смерть
│       ├── rabbitsReproductionSystem.ts  # Размножение кроликов
│       ├── scoringSystem.ts    # Начисление очков
│       └── levelSystem.ts      # Проверка конца уровня, определение победителя
├── ai/
│   ├── vision.ts               # Генерация vision-матрицы, rotateToWorld
│   └── botController.ts        # Решение бота, getBotDirection
├── renderer/
│   └── canvasRenderer.ts       # Отрисовка на canvas (с цветовыми константами)
└── storage/
    └── scoreStorage.ts         # localStorage: очки + имена игроков

tests/
├── __mocks__/styleMock.js      # Мок для CSS-импортов
└── smoke.test.ts               # Smoke-тесты (board, collision, snake, формулы)

docs/
├── rules.md                    # Правила игры (англ.)
├── ai.md                       # Описание AI (англ.)
└── spec.md                     # ← Этот файл (ТЗ на русском)
```

### Принципы

- **Engine** (чистая логика) отделён от **Renderer** (canvas)
- Сущности (змейки, кролики, стены) хранятся отдельно; `board[][]` собирается из них каждый тик
- CSS custom properties для цветов; в canvas — объект `COLORS` с аналогичными константами

---

## 10. Игровой цикл (tick-based)

На каждом тике (`TICK_INTERVAL_MS = 150ms`):

1. Ввод игроков (из `InputHandler`) + решения ботов
2. Применение направлений (с запретом разворота на 180°)
3. Движение змеек
4. Проверка коллизий (стена → смерть, другая змейка → смерть, самоукус → смерть)
5. Поедание кролика → рост + очко + сброс голода
6. Голод / усыхание / смерть от голода
7. Размножение кроликов
8. Сборка `board[][]` из сущностей
9. Рендер (canvas)
10. Проверка конца уровня / игры

**Таймер уровня**: отдельный `setInterval(1000ms)` уменьшает `levelTimeLeft`.

---

## 11. Тесты (Jest)

### Обязательные unit-тесты

- [ ] Запрет разворота на 180°
- [ ] Коллизия со стеной
- [ ] Коллизия с другой змейкой
- [ ] Самоукус
- [ ] Голод: потеря сегмента через 15 тиков
- [ ] Голод: смерть при длине < 2
- [ ] Спавн кроликов: не на стенах, Chebyshev distance > 1
- [ ] Размножение: окно 5–15, сброс clockNum, лимит 5
- [ ] Размножение: штраф за соседей
- [ ] Стены: BFS-проверка связности (нет изолированных зон)
- [ ] Бот: не выбирает смертельный ход при наличии безопасного
- [ ] processTick: полный цикл (движение + коллизия + еда)
- [ ] Формулы: targetScore, rabbitCount, wallClusters, wallLength

---

## 12. Этапы разработки и прогресс

### Этап 1: Документация ✅

| Задача | Статус |
|--------|--------|
| README.md | ✅ Готово |
| docs/rules.md | ✅ Готово |
| docs/ai.md | ✅ Готово |
| docs/spec.md (это ТЗ) | ✅ Готово |

**Коммиты:**
- `ea21b2f` docs: add project documentation

---

### Этап 2: Скелет проекта и сборка ✅

| Задача | Статус |
|--------|--------|
| package.json (зависимости) | ✅ Готово |
| webpack.config.js (dev + prod) | ✅ Готово |
| tsconfig.json | ✅ Готово |
| jest.config.js (ts-jest + jsdom) | ✅ Готово |
| .gitignore | ✅ Готово |
| Структура src/ (все модули-скелеты) | ✅ Готово |
| Smoke-тесты (6 шт.) | ✅ Готово |
| `npm run build` ✓ | ✅ |
| `npm test` ✓ (6/6) | ✅ |

**Коммиты:**
- `d5b0d63` feat: project skeleton with webpack, typescript, jest and full module structure

---

### Этап 3: 1 игрок + тесты 🔧 В работе

| Задача | Статус |
|--------|--------|
| Game loop (processTick) | ✅ Готово |
| initLevel (стены → змейки → кролики) | ✅ Готово |
| InputHandler (WASD + стрелки) | ✅ Готово |
| Canvas renderer (сущности, цвета змеек) | ✅ Готово |
| Интеграция в index.ts (старт, цикл, уровни, результаты) | ✅ Готово |
| Русский интерфейс (меню, HUD, результаты) | ✅ Готово |
| Grid-вёрстка меню (выравнивание) | ✅ Готово |
| Dropdown имён из localStorage | ✅ Готово |
| CSS-классы + custom properties (без inline) | ✅ Готово |
| docs/spec.md (ТЗ на русском) | ✅ Готово |
| **Тесты для 1 игрока** | ❌ Не начато |

**Нужно дописать тесты:**
- Движение змейки (getNextHeadPosition, moveSnake)
- Запрет разворота на 180°
- Коллизии (стена, самоукус)
- Голод (потеря сегмента, смерть)
- Поедание кроликов (рост, очки, сброс голода)
- processTick (интеграционный тест)
- Спавн кроликов (дистанция, не на стенах)
- Генерация стен (BFS-связность)
- Формулы (уже в smoke-тестах)
- checkLevelComplete (одиночная игра)

**Коммиты:**
- `78fa74a` feat: Russian UI, aligned menu layout, name dropdown from localStorage
- `02c4f22` feat: game loop, input handler, canvas renderer with snake colors
- `f5e7a0c` refactor: extract inline styles to CSS classes with color variables

---

### Этап 4: 2 игрока + тесты ⏳ Не начато

| Задача | Статус |
|--------|--------|
| Одновременная игра 2 игроков | ⏳ |
| HUD для 2 змеек | ⏳ |
| Столкновение змеек друг с другом | ⏳ |
| Логика победы уровня (выживший, ничья, побед больше) | ⏳ |
| Тесты: коллизии между змейками, определение победителя | ⏳ |

---

### Этап 5: 1 бот + vision + heuristic ⏳ Не начато

| Задача | Статус |
|--------|--------|
| Генерация vision-матрицы (20×20, повёрнутая) | ⏳ Скелет есть |
| rotateToWorld утилита | ⏳ Скелет есть |
| Эвристический алгоритм (3 хода, flood-fill, притяжение) | ⏳ Скелет есть |
| getBotDirection (перевод решения в Direction) | ⏳ Скелет есть |
| Интеграция ботов в processTick | ⏳ |
| Тесты: бот не выбирает смертельный ход | ⏳ |

---

### Этап 6: 2 бота (обход друг друга) ⏳ Не начато

| Задача | Статус |
|--------|--------|
| Боты учитывают друг друга в vision | ⏳ |
| Обход тел других змеек | ⏳ |
| Тесты: выживаемость ботов в паре | ⏳ |

---

### Этап 7: 4 бота + стресс-тесты ⏳ Не начато

| Задача | Статус |
|--------|--------|
| 4 бота одновременно | ⏳ |
| Стресс-тест: боты выживают N тиков | ⏳ |
| Оптимизация производительности | ⏳ |

---

### Этап 8: Смешанный режим, localStorage, полировка ⏳ Не начато

| Задача | Статус |
|--------|--------|
| Любая комбинация игроков + ботов (0–2 + 0–4) | ⏳ |
| Полная запись/чтение результатов в localStorage | ⏳ Частично |
| Переход между уровнями (carry-over stats) | ⏳ Частично |
| Экран результатов с таблицей рекордов | ⏳ Частично |
| Полировка UI, edge cases | ⏳ |
| Финальная проверка всех тестов | ⏳ |

---

## Уточнения заказчика (лог)

1. **Округление**: `Math.floor()` для всех формул ✅
2. **Максимум 2 игрока** (не 3), управление WASD и стрелки ✅
3. **0 игроков**: режим наблюдателя — боты играют, пользователь смотрит ✅
4. **Стены**: не изолированные комнаты, минимум 2 прохода, BFS-проверка связности ✅
5. **difficultyLevel**: 1–10 ✅
6. **Победитель**: выживший → по побед → по кроликам ✅
7. **Интерфейс** полностью на русском ✅
8. **Вёрстка меню**: grid, правый край ровный, кнопка Start по центру ✅
9. **Имена**: сохранять в localStorage, выпадающий список для выбора ✅
10. **Стили**: CSS-классы + custom properties, без inline ✅
11. **Цель по кроликам кумулятивная**: очки копятся через уровни, цель = сумма целей 1..N ✅
12. **difficultyLevel по умолчанию = 1** ✅
13. **Цвет сетки** → светло-серый (#cccccc) ✅
