# Полный реестр тест-сценариев

Документ фиксирует полный список сценариев по ТЗ и текущую степень покрытия автотестами.

Формат каждого сценария:
- **Что проверяем** — суть проверки и входные условия.
- **Критерий успеха** — однозначный ожидаемый результат.
- **Статус** — `✅ Реализован`, `🟡 Частично`, `❌ Не реализован`.
- **Покрытие** — файл(ы) тестов.

---

## Engine: поле, движение, коллизии

### ENG-01 — Размер пустого поля
- **Что проверяем:** `createEmptyBoard(width, height)`.
- **Критерий успеха:** матрица размером `height x width`, все значения `' '`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`.

### ENG-02 — Проверка границ
- **Что проверяем:** `inBounds` для валидных и невалидных координат.
- **Критерий успеха:** внутри поля `true`, вне поля `false`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`.

### ENG-03 — Следующая клетка головы
- **Что проверяем:** `getNextHeadPosition` для `up/down/left/right`.
- **Критерий успеха:** смещение ровно на 1 клетку в корректной оси.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-04 — Движение без роста
- **Что проверяем:** `moveSnake(snake, false)`.
- **Критерий успеха:** новая голова добавляется, хвост удаляется, длина прежняя.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-05 — Движение с ростом
- **Что проверяем:** `moveSnake(snake, true)`.
- **Критерий успеха:** новая голова добавляется, хвост не удаляется, длина +1.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-06 — Запрет разворота 180° (utility)
- **Что проверяем:** `isReverseDirection`.
- **Критерий успеха:** противоположные направления -> `true`, поворот на 90° -> `false`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`.

### ENG-07 — Запрет разворота 180° (применение к змейке)
- **Что проверяем:** `applyDirection` / `SnakeEntity.applyDirection`.
- **Критерий успеха:** разворот игнорируется, допустимый поворот применяется.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-08 — Столкновение со стеной и границей
- **Что проверяем:** `collidesWithWall`.
- **Критерий успеха:** за границей или на стене -> `true`, на свободной клетке -> `false`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-09 — Столкновение с живой змейкой
- **Что проверяем:** `collidesWithSnake` для живых змей.
- **Критерий успеха:** совпадение с сегментом живой змейки возвращает `true`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-10 — Игнорирование мертвой змейки
- **Что проверяем:** `collidesWithSnake` для `alive=false`.
- **Критерий успеха:** совпадение с мертвой змейкой не считается коллизией.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-11 — Самоукус
- **Что проверяем:** `selfCollision`.
- **Критерий успеха:** совпадение головы с телом -> `true`, иначе `false`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

---

## Engine: голод, кролики, стены, формулы

### ENG-12 — Голод: инкремент до порога
- **Что проверяем:** `processHunger` до `hungerThreshold`.
- **Критерий успеха:** `ticksWithoutFood` растет, длина не меняется.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-13 — Голод: потеря сегмента
- **Что проверяем:** `processHunger` на пороге.
- **Критерий успеха:** длина -1, счетчик голода сброшен.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-14 — Голод: смерть при длине < minSnakeLength
- **Что проверяем:** многократное укорачивание до критической длины.
- **Критерий успеха:** `alive=false`, причина `'Умерла с голоду'`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-15 — Сброс голода
- **Что проверяем:** `resetHunger` и сброс после еды.
- **Критерий успеха:** `ticksWithoutFood = 0`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-16 — Начальный спавн еды
- **Что проверяем:** `spawnFood(count, state, ctx)`.
- **Критерий успеха:** создается нужное количество (или меньше при нехватке места).
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-17 — Валидность позиций еды при спавне
- **Что проверяем:** еда не на стенах, не на змейках, не вне поля.
- **Критерий успеха:** все позиции валидны.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-18 — Дистанция между едой
- **Что проверяем:** расстояние Чебышева между любой парой еды.
- **Критерий успеха:** всегда `> 1`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-19 — Параметры новой еды
- **Что проверяем:** `age`, `clockNum`, `reproductionCount`.
- **Критерий успеха:** все три поля `0`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`, `tests/engine-rabbits-reproduction.test.ts`.

### ENG-20 — Фазы еды (young/adult/old)
- **Что проверяем:** `getFoodPhase` на порогах возрастов.
- **Критерий успеха:** фазы совпадают с `foodYoungAge`/`foodAdultAge`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### ENG-21 — Удаление еды по возрасту
- **Что проверяем:** удаление `age >= foodMaxAge`.
- **Критерий успеха:** старая еда исключается из `state.foods`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### ENG-22 — Размножение: кулдаун и лимит размножений
- **Что проверяем:** `reproductionMinCooldown`, `maxReproductions`.
- **Критерий успеха:** до кулдауна размножения нет; при исчерпанном лимите размножения нет.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### ENG-23 — Размножение: лимит соседей
- **Что проверяем:** блокировка при `nearbyCount >= maxReproductionNeighbors`.
- **Критерий успеха:** потомство не появляется.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### ENG-24 — Размножение: дистанция 1..2 от родителя
- **Что проверяем:** расположение потомка.
- **Критерий успеха:** расстояние Чебышева от родителя в диапазоне `[1,2]`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### ENG-25 — Размножение: валидность позиции потомка
- **Что проверяем:** не на стенах/змейках/рядом с другой едой.
- **Критерий успеха:** `isValidFoodPosition` соблюдается.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-rabbits-reproduction.test.ts`.

### FOOD-01 — Появление куриной еды
- **Что проверяем:** выбор типа еды при начальном спавне и автодобавлении.
- **Критерий успеха:** на уровне 1 всегда создаются яблоки; с уровня 2 seeded RNG даёт 30% `ChickenFood`; штатно созданная куриная еда всегда является яйцом с `age = 0`; обычное размножение сохраняет тип родителя.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-chicken-food.test.ts`.

### FOOD-02 — Стадии и ценность куриной еды
- **Что проверяем:** яйцо `0..49`, цыплёнок `50..99`, курица `100..149`.
- **Критерий успеха:** используются значки 🥚/🐤/🐔, а очки и рост равны соответственно `1/2/3`; на тике 150 взрослая курица превращается в мясо.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-chicken-food.test.ts`, `tests/ui-output-safety.test.ts`.

### FOOD-03 — Движение цыплёнка и курицы
- **Что проверяем:** интервалы, радиусы, приоритеты и занятость клеток.
- **Критерий успеха:** цыплёнок ходит раз в 3 тика и остаётся в радиусе 5 от яйца; курица может ходить раз в 2 тика без общего радиуса, приоритетно идёт на соседнее яблоко, иначе убегает при змейке в радиусе 5; одинаковый seed воспроизводит выборы; сущности не накладываются.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-chicken-food.test.ts`.

### FOOD-04 — Кладка яиц взрослой курицей
- **Что проверяем:** обычную и обязательную кладку.
- **Критерий успеха:** обычная кладка в фазе `old` использует общие вероятность/cooldown/лимит, но игнорирует плотность еды; после яблока следующая кладка игнорирует также вероятность, cooldown и исчерпанный лимит, а при отсутствии места остаётся ожидающей.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-chicken-food.test.ts`.

### FOOD-05 — Поедание яблока курицей
- **Что проверяем:** эффекты перехода курицы на яблоко.
- **Критерий успеха:** яблоко удалено без очков, у курицы `age = 100`, `reproductionCount = 0`, обязательная кладка выполняется не раньше следующего тика и после успеха устанавливает `reproductionCount = 1`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-chicken-food.test.ts`.

### FOOD-06 — Стадия и поедание мяса
- **Что проверяем:** жизненный цикл и награду `MeatFood`.
- **Критерий успеха:** мясо неподвижно, существует при `age 0..49`, удаляется на `age = 50`, не спавнится и не размножается штатно; поедание даёт +1 очко, +1 сегмент и сбрасывает голод.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-meat-food.test.ts`, `tests/ui-output-safety.test.ts`.

### FOOD-07 — Появление мяса после смерти
- **Что проверяем:** смерть курицы и змейки.
- **Критерий успеха:** курица на `age = 150` заменяется одним мясом на своей клетке; смерть змейки от столкновения или самоукуса однократно создаёт `ceil(length / 3)` кусков по группам тела при наличии места; смерть от голода не создаёт мясо; повторный тик не дублирует выпадение.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-meat-food.test.ts`.

### ENG-26 — Формула цели уровня
- **Что проверяем:** `getTargetScore`.
- **Критерий успеха:** `Math.floor(coeff * level + base)`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`, `tests/engine-implemented.test.ts`.

### ENG-27 — Формула кумулятивной цели
- **Что проверяем:** `getCumulativeTargetScore`.
- **Критерий успеха:** сумма целей уровней `1..N`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`, `tests/engine-implemented.test.ts`.

### ENG-28 — Формула начального количества еды
- **Что проверяем:** `getInitialFoodCount`.
- **Критерий успеха:** значение строго по формуле с `Math.floor`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`, `tests/engine-implemented.test.ts`.

### ENG-28a — Канонические JSON-дефолты
- **Что проверяем:** загрузку значений из `packages/core/src/gameDefaults.json`.
- **Критерий успеха:** `foodCountBase = 5`, `reproductionProbabilityBase = 0.01`, `levelSizeIncrement = 2`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`.

### ENG-29 — Формула количества кластеров стен
- **Что проверяем:** `getWallClusterCount`.
- **Критерий успеха:** расчет по формуле с `Math.floor`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-30 — Формула длины стен
- **Что проверяем:** `getWallLength`.
- **Критерий успеха:** расчет по формуле с `Math.floor`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-31 — `validateWalls` для пустых стен
- **Что проверяем:** `validateWalls([], w, h)`.
- **Критерий успеха:** возвращает `true`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-32 — `validateWalls` для изолированной области
- **Что проверяем:** стенка, разделяющая поле на изолированные зоны.
- **Критерий успеха:** возвращает `false`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-33 — `generateWalls`: валидные стены или fallback
- **Что проверяем:** результат генерации после валидации.
- **Критерий успеха:** `validateWalls(result) === true` либо `result = []`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-34 — `generateWalls`: зона безопасности
- **Что проверяем:** дистанция от exclusion zones.
- **Критерий успеха:** ни одна стена не попадает в safe radius.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

---

## Engine: интеграция тика и завершение уровня

### ENG-35 — Инициализация уровня
- **Что проверяем:** `GameEngine.initLevel`.
- **Критерий успеха:** корректная переинициализация уровня (стены, змейки, кролики, флаги, счетчики).
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/board-singleplayer.integration.test.ts`, `tests/app-implemented.test.ts`.

### ENG-36 — `processTick`: базовое движение
- **Что проверяем:** обычный тик без столкновений.
- **Критерий успеха:** `tickCount++`, голова движется на 1 клетку.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-37 — `processTick`: смерть от стены
- **Что проверяем:** выход за границу/попадание в стену в новом ходе.
- **Критерий успеха:** змейка умирает с причиной `'Врезалась в стену'`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-38 — `processTick`: поедание кролика
- **Что проверяем:** рост, очки, удаление кролика, сброс голода.
- **Критерий успеха:** все четыре эффекта происходят за тик.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-39 — `processTick`: вход в собственный хвост
- **Что проверяем:** кейс, где хвост сдвигается в том же тике.
- **Критерий успеха:** змейка остается живой (валидный ход).
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`.

### ENG-40 — Порядок систем тика
- **Что проверяем:** порядок фаз пайплайна.
- **Критерий успеха:** движение -> голод -> кролики -> board -> level check.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

### ENG-41..ENG-46 — Завершение уровня и победители
- **Что проверяем:** `checkLevelComplete`, `getLevelWinner`, `getOverallWinner`.
- **Критерий успеха:** одиночный и мультиплеерный режимы завершаются по ТЗ, победитель определяется по приоритету побед/очков.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/engine-implemented.test.ts`, `tests/board-singleplayer.integration.test.ts`, `tests/board-multiplayer.integration.test.ts`.

### ENG-47 — Бонус за выживание после завершения раунда
- **Что проверяем:** однократное начисление длины живым змейкам после завершения раунда.
- **Критерий успеха:** `score += segments.length` для каждой живой змейки; погибшие не получают бонус; при завершении по таймеру бонус получают все выжившие; бонус входит в `scoreGained` и переносится на следующий раунд без повторного начисления.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

---

## App: ввод, сервисы, FSM, scheduler, storage/settings

### APP-01 — Буфер ввода: последнее допустимое нажатие за тик
- **Что проверяем:** `InputHandler.consumeAll` и применение накопленной очереди.
- **Критерий успеха:** берётся последнее направление, не являющееся разворотом на 180°; более поздняя недопустимая команда не стирает предыдущую допустимую; буфер очищается.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-02 — Space как отдельная команда
- **Что проверяем:** `InputHandler` обработка `Space`.
- **Критерий успеха:** вызывается pause callback, направление не записывается.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-03 — Применение ввода игроков
- **Что проверяем:** `InputApplicationService` для людей.
- **Критерий успеха:** направление применяется только живым активным игрокам.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-04 — Применение ввода ботов
- **Что проверяем:** `InputApplicationService` + `processBots`.
- **Критерий успеха:** применяются корректные направления по `snake.id`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-05 — Переход на следующий уровень
- **Что проверяем:** `SessionProgressionService.advanceToNextLevel`.
- **Критерий успеха:** `level+1`, новые сущности и размер поля, сброс тика/таймера, перенос `score` и `levelsWon`, включая автоматический переход survival.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-06 — Сохранение результатов сессии
- **Что проверяем:** `ScorePersistenceService.saveSessionScores`.
- **Критерий успеха:** результаты сохраняются для всех змеек с корректными полями; повторный вызов для того же состояния не создаёт дубликатов.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-07 — Экспорт/импорт runtime-настроек
- **Что проверяем:** `settingsToJSON`, `applyJSONToSettings`, `resetSettings`, overrides.
- **Критерий успеха:** значения корректно сериализуются, применяются и сбрасываются.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-08 — LocalStorage helper для очков/имен
- **Что проверяем:** `saveScore/getScores/clearScores`, `saveName/getSavedNames`.
- **Критерий успеха:** сортировка очков, очистка, дедупликация имен, лимиты и trim.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-09 — FSM переходы и `Space`-логика
- **Что проверяем:** `GameFSM.send`, `handleSpace`, invalid transitions.
- **Критерий успеха:** переходы соответствуют таблице FSM, недопустимые события возвращают `null`.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-10 — Планировщик тиков
- **Что проверяем:** `GameLoopScheduler.start/stop/isRunning`.
- **Критерий успеха:** двойной `start` не создает дубликатов, `stop` останавливает оба интервала.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-10a — Таймер и изоляция настроек уровня
- **Что проверяем:** уменьшение таймера через Engine и разрешение per-level settings.
- **Критерий успеха:** App не мутирует таймер напрямую; override применяется только к выбранному уровню и не изменяет базовые настройки.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-11 — Выбор модалки при завершении уровня
- **Что проверяем:** `LevelCompletionService` в обычном и dev режиме.
- **Критерий успеха:** корректный выбор между `LevelComplete`/`GameOver` и нужными callback.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts`.

### APP-12 — Keyboard actions: Escape / Enter
- **Что проверяем:** обработку `Escape` и `Enter` в `InputHandler` и `GameController`.
- **Критерий успеха:** `Escape` вызывает escape callback; `Enter` вызывает confirm callback; в `GameController` выполняются сценарии выхода из pause и подтверждения выхода в меню.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`.

### APP-13 — Двухпользовательский ввод в одном тике
- **Что проверяем:** одновременное применение команд двух игроков (`WASD` + стрелки) в `InputHandler` и `InputApplicationService`.
- **Критерий успеха:** команды обоих игроков применяются в одном тике независимо, направления и позиции обновляются синхронно.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/app-implemented.test.ts`, `tests/board-multiplayer.integration.test.ts`.

### APP-14 — Интеграционные сценарии одновременного движения
- **Что проверяем:** head-on столкновение и вход в освобождаемый хвост другой змейки.
- **Критерий успеха:** при ходе голов в одну клетку погибают обе змейки с событием `SNAKE_DIED`; вход в освобождаемую клетку хвоста разрешён и не зависит от порядка змей в массиве.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/board-multiplayer.integration.test.ts`.

### APP-15 — Смешанный режим
- **Что проверяем:** инициализацию и тик комбинации из одного человека и двух ботов.
- **Критерий успеха:** создаются три змейки с корректными ролями, команды человека и AI применяются в одном тике без нарушения состояния.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/board-multiplayer.integration.test.ts`.

---

## UI / E2E (DOM-ориентированные сценарии)

### UI-01 — Меню: ограничение диапазонов
- **Что проверяем:** нормализацию `playerCount`, `botCount`, `difficultyLevel`.
- **Критерий успеха:** значения всегда в диапазонах ТЗ.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

### UI-02 — Dropdown имен в меню
- **Что проверяем:** открытие, выбор, закрытие при клике вне.
- **Критерий успеха:** выбранное имя подставляется, dropdown корректно закрывается.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

### UI-03 — Пауза по Space (сквозной сценарий)
- **Что проверяем:** `GameController + FSM + modal + loop`.
- **Критерий успеха:** пауза/возобновление работает полностью в реальном UI потоке.
- **Статус:** `🟡 Частично`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts` (FSM-уровень), нет e2e через `GameController`.

### UI-04 — Пауза замораживает тик и таймер
- **Что проверяем:** отсутствие изменений `tickCount` и `levelTimeLeft` в паузе.
- **Критерий успеха:** оба счетчика не изменяются до resume.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

### UI-05 — Модалка конца уровня
- **Что проверяем:** показ и обработка Continue/Space.
- **Критерий успеха:** вызывается корректная модалка и правильный callback перехода.
- **Статус:** `🟡 Частично`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts` (service-level, без DOM-контента).

### UI-06 — Модалка game over -> results
- **Что проверяем:** ветка обычного game over.
- **Критерий успеха:** выбирается `GameOver` и показывается корректная game-over модалка с обработкой действий.
- **Статус:** `🟡 Частично`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts` (service-level).

### UI-07 — Dev overlay покрывает только game-area
- **Что проверяем:** размещение overlay в dev layout.
- **Критерий успеха:** dev-панель справа остается интерактивной.
- **Статус:** `❌ Не реализован`.
- **Покрытие:** —.

### UI-08 — Dev game-over без экрана результатов
- **Что проверяем:** dev ветка завершения.
- **Критерий успеха:** выбирается restart same level, без перехода на results.
- **Статус:** `🟡 Частично`.
- **Покрытие:** `tests/app-settings-storage-fsm.test.ts` (service-level).

### UI-09 — HUD (раскладка и данные)
- **Что проверяем:** данные, цвет статистики и запуск быстрого доигрывания смешанной игры.
- **Критерий успеха:** статистика соответствует state и цвету змейки, после смерти становится серой; кнопка «Быстро доиграть» доступна после смерти всех людей при живых ботах.
- **Статус:** `🟡 Частично`.
- **Покрытие:** `tests/ui-output-safety.test.ts` (данные, цвет и кнопка; без проверки геометрии раскладки).

### UI-10 — Безопасный вывод пользовательских данных
- **Что проверяем:** вывод имени игрока в HUD и на экране результатов.
- **Критерий успеха:** HTML в имени отображается как текст и не создаёт DOM-элементы.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/ui-output-safety.test.ts`.

---

## AI: логика бота и vision

### AI-00 — Детерминированность алгоритмов Arena
- **Что проверяем:** случайную политику и инициализацию простой нейросети при одинаковом seed.
- **Критерий успеха:** одинаковый seed создаёт одинаковые веса сети и одинаковые метрики headless-прогона.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/training-random-algorithm.test.ts`, `tests/ai/simpleNetwork.test.ts`.

### AI-01 — Избежание гарантированной смерти
- **Что проверяем:** выбор направления в опасной ситуации.
- **Критерий успеха:** бот не выбирает смертельный ход, если есть безопасный.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/heuristic-greedy.test.ts`.

### AI-02 — Ориентация vision-матрицы
- **Что проверяем:** корректность поворота координат в vision.
- **Критерий успеха:** "вперед" всегда нормализовано как верх матрицы.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/ai-vision.test.ts`.

### AI-03 — Сигналы препятствий/кроликов
- **Что проверяем:** знак и затухание сигналов.
- **Критерий успеха:** параметры соответствуют настройкам и формулам.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/ai-vision.test.ts`.

### AI-04 — Стресс-тест 4 ботов
- **Что проверяем:** стабильность длительной симуляции.
- **Критерий успеха:** без исключений, инварианты state не нарушаются.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/heuristic-arena.long.test.ts`.

### AI-05 — Вероятностные ошибки ботов
- **Что проверяем:** проверку ошибки после расчёта оптимального действия для профилей `rookie`, `basic`, `solid`, `wise`.
- **Критерий успеха:** вероятность определяется как `1 / mistakePeriod` и уменьшается с ростом сложности; seeded RNG воспроизводит последовательность; два исхода выбираются 50/50 — случайное другое абсолютное направление, включая запрещённое, либо правильная команда с задержкой ровно на один тик; на тике задержанной команды нет нового решения/ошибки; ошибочный ход может быть смертельным и не заменяется безопасным fallback.
- **Статус:** `🟡 Частично`: существует периодический выбор ухудшенного несмертельного направления без RNG, задержанной команды и ошибок профиля `wise`.
- **Покрытие:** `tests/heuristic-greedy.test.ts` покрывает только текущее частичное поведение.

---

## Smoke

### SMK-01 — Базовые smoke-проверки
- **Что проверяем:** минимальные инварианты сборки и ядра.
- **Критерий успеха:** быстрый, стабильный прогон на чистой среде.
- **Статус:** `✅ Реализован`.
- **Покрытие:** `tests/smoke.test.ts`.
